import { Config } from '@react-native-community/cli-types';
import { KeplerBundleArgs } from './types';
declare function buildKeplerBundle(ctx: Config, args: KeplerBundleArgs): Promise<void>;
declare function generatePathsAndNames(root: string, entryFileName: string, buildType: string): {
    bundleDir: string;
    srcMapDir: string;
    bundlePath: string;
    generatedSrcMapPath: string;
    hermesBundlePath: string;
};
export { buildKeplerBundle, KeplerBundleArgs, generatePathsAndNames };

export declare class BundleUtils {
    static readonly keplerManifestBuilderPackage = "@amazon-devices/kepler-module-manifest-builder";
    private static readonly keplerManifestBuilderBin;
    static readonly systemBundleModuleConfigFile = "kepler-system-conf.toml";
    static readonly splitBundleToolPackage = "@amazon-devices/keplerscript-commonmodules";
    static readonly rnCLIPackage = "@react-native-community/cli";
    private static readonly minimumCLIVersion;
    private static readonly metroConfigPackage;
    static getBundlerWithConfig(projectRoot: string, reactNativeVersion: string): any;
    static getMetroLoadConfig(projectRoot: string, reactNativeVersion: string): any;
    static getMetroMergeConfig(projectRoot: string): any;
    static getKeplerManifestBuilderBin(projectRoot: string): string;
    static computeModulesRoots(projectRoot: string): string[];
    /**
     * Helper to resolve a binary from a package's bin entry in package.json
     */
    private static resolveBinaryFromPackage;
    private static getBundlerFunction;
    /**
     * Helper function to validate that the RN CLI version that is seen (first seen, i.e. in "root/node_modules")
     * is of the minimum version. To avoid a empty RN version in "react-native config".
     * @param projectRoot Root of the project
     * @returns boolean if RN CLI package is of the minimum version. "True" being a valid CLI version.
     */
    static validateRnCLIVersion(projectRoot: string): boolean;
    /**
     * Resolves a bundler module based on the given react native version.
     * @param projectRoot
     * @param reactNativeVersion react native version
     * @returns {bundlerInfo, rootModulePath} returns rootModulePath to avoid finding module path again
     */
    private static getBundleModulePath;
    private static getModulePath;
    private static resolveFrom;
}

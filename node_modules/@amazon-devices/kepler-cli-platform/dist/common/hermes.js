"use strict";
/*
 * Copyright (c) 2024 Amazon.com, Inc. or its affiliates.  All rights reserved.
 *
 * PROPRIETARY/CONFIDENTIAL.  USE IS SUBJECT TO LICENSE TERMS.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Hermes = void 0;
const keplerExecutor_1 = require("./keplerExecutor");
const fs_1 = __importStar(require("fs"));
const path_1 = __importDefault(require("path"));
const shelljs_1 = __importDefault(require("shelljs"));
const moduleUtils_1 = require("./moduleUtils");
const log_1 = __importDefault(require("./log"));
const os_1 = require("os");
const cli_tools_1 = require("@react-native-community/cli-tools");
class Hermes {
    static async createBundle(ctx, args) {
        fs_1.default.mkdirSync(path_1.default.dirname(args.out), { recursive: true });
        const hermesCmd = [
            Hermes.getHermesPath(ctx),
            '--emit-binary',
            '-out',
            args.out,
            args.bundlePath,
            ...Hermes.parseOptions(args.hermesFlags),
        ];
        const result = shelljs_1.default.exec(hermesCmd.join(' '), { silent: true });
        if (result.code !== 0) {
            throw new cli_tools_1.CLIError(`An error occurred while executing Hermes command ${hermesCmd}: ${result.stderr}`);
        }
    }
    static parseOptions(hermesOpts) {
        return hermesOpts.split(',');
    }
    static getHermesPath(ctx) {
        if (!Hermes.hermesPath) {
            Hermes.hermesPath = this.getHermesPathHelper(ctx);
        }
        return Hermes.hermesPath;
    }
    static getHermesPathHelper(ctx) {
        var _a, _b;
        let rnKeplerPackagePath = '';
        const reactNativeKeplerPkg = ((_b = (_a = ctx.platforms) === null || _a === void 0 ? void 0 : _a.kepler) === null || _b === void 0 ? void 0 : _b.npmPackageName) || '@amazon-devices/react-native-kepler';
        log_1.default.debug(`Using ${reactNativeKeplerPkg} to find hermes binary.`);
        try {
            rnKeplerPackagePath = moduleUtils_1.ModuleUtils.getModulePath(ctx.root, reactNativeKeplerPkg, false);
        }
        catch (e) {
            throw new cli_tools_1.CLIError(`Error: Required '${reactNativeKeplerPkg}' module's path could not be resolved. Ensure Npm package is properly installed.\n` +
                `Full Error: ${e.message}`);
        }
        let platformDependentDir = '';
        const hostOs = (0, os_1.platform)();
        if (hostOs === 'linux') {
            platformDependentDir = 'linux64-bin';
        }
        else if (hostOs === 'darwin') {
            platformDependentDir = 'osx-bin';
        }
        else {
            throw new cli_tools_1.CLIError(`Unsupported host OS: ${hostOs}`);
        }
        let hermesBinaryPath = path_1.default.join(rnKeplerPackagePath, 'sdks', 'hermesc', platformDependentDir, 'hermesc');
        if (!(0, fs_1.existsSync)(hermesBinaryPath)) {
            log_1.default.debug(`Hermes binary not found within ${reactNativeKeplerPkg}: '${hermesBinaryPath}'. Using 'kepler path hermes' command...`);
            hermesBinaryPath = keplerExecutor_1.KeplerExecutor.getHermesPath();
        }
        log_1.default.debug(`Hermes binary found in '${hermesBinaryPath}'.`);
        return hermesBinaryPath;
    }
}
exports.Hermes = Hermes;
Hermes.hermesPath = null;

import { BuildArgs } from './types';
export interface ShellOptions {
    fatal?: boolean;
    silent?: boolean;
}
/**
 * Contains the various commands to be executed by KeplerNative build system.
 */
export declare class KeplerExecutor {
    private static readonly KEPLER_NPM_COMMAND;
    private static readonly defaultBuildToolEnvVarMap;
    /**
     * Mapping of RN plugin args to Kepler Native args
     */
    private static readonly buildArgsMap;
    private static readonly buildArgsKeys;
    private static readonly KEPLER_CLI_NAME;
    private static readonly KEPLER_ENV_NAME;
    static build(options: string): void;
    static getBaseNpmCommand(): string;
    static getHermesPath(): string;
    /**
     * Create the build options based on json input
     * @param args Json input received from resource
     */
    static createBuildOptions(args: BuildArgs): string;
    /**
     * Trigger a build with Kepler Native
     * @param options cli args to pass to Kepler Native
     * @throws an error if build command executed by shell returns non-zero.
     */
    private static exec;
    private static parseToolPathOutput;
    private static getKeplerExecutable;
}

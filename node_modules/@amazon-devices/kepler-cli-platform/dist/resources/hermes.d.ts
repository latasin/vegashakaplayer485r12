import { Resource } from '../resource';
interface HermesKeplerArgs {
    out: string;
    bundlePath: string;
    hermesFlags: string;
}
declare const hermes: Resource<void, HermesKeplerArgs>;
export default hermes;

"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.writeKeplerManifest = exports.buildKeplerManifest = exports.getPolyfillPath = void 0;
const child_process = __importStar(require("child_process"));
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const logger_1 = require("./logger");
function getPolyfillPath(dirname = __dirname) {
    return path.resolve(dirname, 'keplerManifestPolyfill.js');
}
exports.getPolyfillPath = getPolyfillPath;
function buildKeplerManifest(manifestBuilderBinary, packageDir = ['.'], packageJsonPath = './package.json') {
    try {
        const packageDirArgs = packageDir.map((arg) => `-d ${arg}`).join(' ');
        const output = child_process.execSync(`${manifestBuilderBinary} build ${packageDirArgs} -p ${packageJsonPath} -s`, { encoding: 'utf8' });
        return JSON.parse(output);
    }
    catch (error) {
        throw new Error(`Unable to generate a manifest: ${error}`);
    }
}
exports.buildKeplerManifest = buildKeplerManifest;
function writeKeplerManifest(manifestBuilderBinary, packageDir = ['.'], packageJsonPath = './package.json') {
    const moduleMapPolyfillFile = getPolyfillPath();
    const keplerManifest = buildKeplerManifest(manifestBuilderBinary, packageDir, packageJsonPath);
    logger_1.logger.log(`[debug] writing to file:\n[debug] global.KEPLER_MODULE_MAP = ${JSON.stringify(keplerManifest.keplerModuleMap || {}, null, 2)};`);
    fs.writeFileSync(moduleMapPolyfillFile, `global.KEPLER_MODULE_MAP = ${JSON.stringify(keplerManifest.keplerModuleMap || {})};`);
    return moduleMapPolyfillFile;
}
exports.writeKeplerManifest = writeKeplerManifest;

"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const child_process = __importStar(require("child_process"));
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const metro_config_1 = require("../src/metro-config");
const utils_1 = require("../src/utils");
jest.mock('child_process', () => ({
    execSync: jest.fn().mockReturnValue(JSON.stringify({
        keplerModuleMap: {
            '@amazon-devices/react-native-gesture-handler': '/com.amazon.kepler.gesture_handler_2@IGestureHandler_0',
        },
    })),
}));
describe('getKeplerCompatibilityMetroConfig', () => {
    beforeEach(() => {
        // Creates a temporary file for testing
        if (!fs.existsSync(path.dirname((0, utils_1.getPolyfillPath)()))) {
            fs.mkdirSync(path.dirname((0, utils_1.getPolyfillPath)()), { recursive: true });
        }
        child_process.execSync.mockClear();
    });
    afterEach(() => {
        // Clean up any files created during tests
        try {
            fs.unlinkSync((0, utils_1.getPolyfillPath)());
        }
        catch (e) {
            // Ignore if file doesn't exist
        }
        jest.resetModules();
    });
    it('should create polyfill file with correct content', () => {
        const config = (0, metro_config_1.getKeplerCompatibilityMetroConfig)();
        expect(fs.existsSync((0, utils_1.getPolyfillPath)())).toBe(true);
        const fileContent = fs.readFileSync((0, utils_1.getPolyfillPath)(), 'utf8');
        expect(fileContent).toContain('global.KEPLER_MODULE_MAP');
        expect(fileContent).toContain('@amazon-devices/react-native-gesture-handler');
    });
    it('should return config with polyfills array', () => {
        const config = (0, metro_config_1.getKeplerCompatibilityMetroConfig)();
        expect(config).toHaveProperty('serializer.getPolyfills');
        const polyfills = config.serializer.getPolyfills();
        expect(Array.isArray(polyfills)).toBe(true);
        expect(polyfills.length).toBe(1);
        expect(polyfills[0]).toBe((0, utils_1.getPolyfillPath)());
    });
    it('should use default values when no parameters are provided', () => {
        (0, metro_config_1.getKeplerCompatibilityMetroConfig)();
        expect(child_process.execSync).toHaveBeenCalledWith(expect.stringContaining('-d .'), expect.any(Object));
        expect(child_process.execSync).toHaveBeenCalledWith(expect.stringContaining('-p ./package.json'), expect.any(Object));
    });
    it('should use custom packageDir when provided', () => {
        // assume ./packages/specific_package contains a node_modules directory
        const customPackageDir = ['./packages/specific_package'];
        (0, metro_config_1.getKeplerCompatibilityMetroConfig)('', customPackageDir);
        expect(child_process.execSync).toHaveBeenCalledWith(expect.stringContaining(`-d ${customPackageDir}`), expect.any(Object));
    });
    it('should use custom packageJsonPath when provided', () => {
        const customPackageJsonPath = './packages/specific_package/package.json';
        (0, metro_config_1.getKeplerCompatibilityMetroConfig)('', ['.'], customPackageJsonPath);
        expect(child_process.execSync).toHaveBeenCalledWith(expect.stringContaining(`-p ${customPackageJsonPath}`), expect.any(Object));
    });
    it('should use all custom parameters when provided', () => {
        const customBinary = './path/to/kepler-module-manifest-builder';
        // assume ./packages/specific_package contains a node_modules directory
        const customPackageDir = ['./packages/specific_package'];
        const customPackageJsonPath = './packages/specific_package/package.json';
        (0, metro_config_1.getKeplerCompatibilityMetroConfig)(customBinary, customPackageDir, customPackageJsonPath);
        expect(child_process.execSync).toHaveBeenCalledWith(`${customBinary} build -d ${customPackageDir} -p ${customPackageJsonPath} -s`, expect.any(Object));
    });
    it('should support an array of packageDirs when provided', () => {
        const customBinary = './path/to/kepler-module-manifest-builder';
        // assume ./packages/specific_package contains a node_modules directory
        const customPackageDir = [
            './packages/specific_package_1',
            './packages/specific_package_2',
            './packages/specific_package_3',
        ];
        const customPackageJsonPath = './packages/specific_package/package.json';
        (0, metro_config_1.getKeplerCompatibilityMetroConfig)(customBinary, customPackageDir, customPackageJsonPath);
        const expectedPackageDirArgs = '-d ./packages/specific_package_1 -d ./packages/specific_package_2 -d ./packages/specific_package_3';
        expect(child_process.execSync).toHaveBeenCalledWith(`${customBinary} build ${expectedPackageDirArgs} -p ${customPackageJsonPath} -s`, expect.any(Object));
    });
});

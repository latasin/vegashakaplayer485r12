"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const child_process = __importStar(require("child_process"));
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const logger_1 = require("../src/logger");
const utils_1 = require("../src/utils");
jest.mock('child_process');
describe('utils', () => {
    beforeEach(() => {
        logger_1.logger.setDebug(true);
    });
    afterEach(() => {
        logger_1.logger.setDebug(false);
    });
    describe('getPolyfillPath', () => {
        it('should return path ending with keplerManifestPolyfill.js', () => {
            const result = (0, utils_1.getPolyfillPath)();
            expect(result.endsWith('keplerManifestPolyfill.js')).toBe(true);
        });
        it('should use __dirname by default', () => {
            const result = (0, utils_1.getPolyfillPath)();
            expect(result).toBe(path.resolve(__dirname, '../src', 'keplerManifestPolyfill.js'));
        });
        it('should accept custom dirname', () => {
            const customDir = '/custom/path';
            const result = (0, utils_1.getPolyfillPath)(customDir);
            expect(result).toBe('/custom/path/keplerManifestPolyfill.js');
        });
        it('should return absolute path', () => {
            const result = (0, utils_1.getPolyfillPath)();
            expect(path.isAbsolute(result)).toBe(true);
        });
    });
    describe('buildKeplerManifest', () => {
        const mockManifest = {
            keplerModuleMap: {
                '@amazon-devices/react-native-gesture-handler': '/com.amazon.kepler.gesture_handler_2@IGestureHandler_0',
            },
        };
        beforeEach(() => {
            child_process.execSync.mockReturnValue(JSON.stringify(mockManifest));
        });
        it('should return parsed manifest when command succeeds', () => {
            const result = (0, utils_1.buildKeplerManifest)('node_modules/.bin/kepler-module-manifest-builder');
            expect(result).toEqual(mockManifest);
        });
        it('should throw Error when command fails', () => {
            const sampleError = 'Command failed';
            child_process.execSync.mockImplementation(() => {
                throw new Error(sampleError);
            });
            expect(() => (0, utils_1.buildKeplerManifest)('node_modules/.bin/kepler-module-manifest-builder')).toThrow(`Unable to generate a manifest: Error: ${sampleError}`);
        });
    });
    describe('writeKeplerManifest', () => {
        const mockManifest = {
            keplerModuleMap: {
                '@amazon-devices/react-native-gesture-handler': '/com.amazon.kepler.gesture_handler_2@IGestureHandler_0',
            },
        };
        beforeEach(() => {
            child_process.execSync.mockReturnValue(JSON.stringify(mockManifest));
        });
        it('should write manifest to file and return file path', () => {
            const result = (0, utils_1.writeKeplerManifest)('node_modules/.bin/kepler-module-manifest-builder');
            expect(fs.existsSync(result)).toBe(true);
            const content = fs.readFileSync(result, 'utf8');
            expect(content).toContain('global.KEPLER_MODULE_MAP');
            expect(content).toContain('@amazon-devices/react-native-gesture-handler');
        });
        it('should handle empty keplerModuleMap', () => {
            child_process.execSync.mockReturnValue(JSON.stringify({}));
            const result = (0, utils_1.writeKeplerManifest)('node_modules/.bin/kepler-module-manifest-builder');
            const content = fs.readFileSync(result, 'utf8');
            expect(content).toBe('global.KEPLER_MODULE_MAP = {};');
        });
    });
});

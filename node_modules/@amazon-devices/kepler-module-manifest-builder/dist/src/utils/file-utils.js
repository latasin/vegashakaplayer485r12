"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCompatibilityForLibraryName = exports.parseJsonFile = exports.extractTarball = void 0;
const child_process_1 = require("child_process");
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const logger_1 = require("./logger");
function extractTarball(npmManager, packageName, version, tempDir) {
    try {
        // Download the package
        (0, child_process_1.execSync)(`${npmManager} pack ${packageName}@${version} --pack-destination ${tempDir}`, {
            encoding: 'utf8',
            stdio: 'pipe',
        });
        // Find the tarball
        const tarballName = fs
            .readdirSync(tempDir)
            .find((file) => file.endsWith('.tgz'));
        if (!tarballName) {
            throw new Error('No tarball found');
        }
        // Extract the tarball
        (0, child_process_1.execSync)(`tar -xzf ${path.join(tempDir, tarballName)} -C ${tempDir}`, {
            encoding: 'utf8',
            stdio: 'pipe',
        });
    }
    catch (error) {
        throw new Error(`Failed to extract NPM package for ${packageName}@${version}: ${error}`);
    }
}
exports.extractTarball = extractTarball;
function parseJsonFile(jsonFilePath) {
    if (!fs.existsSync(jsonFilePath)) {
        logger_1.logger.error('file not found, verify your path is valid: ', jsonFilePath);
        throw new Error(`Provided file not found: ${jsonFilePath}`);
    }
    try {
        return JSON.parse(fs.readFileSync(jsonFilePath, 'utf-8'));
    }
    catch (error) {
        logger_1.logger.error(`Failed to read ${jsonFilePath}:`, error);
        throw error;
    }
}
exports.parseJsonFile = parseJsonFile;
function getCompatibilityForLibraryName(nodeModulesPath, libraryName) {
    const libraryPath = path.join(nodeModulesPath, libraryName);
    const compatibilityPath = path.join(libraryPath, 'kepler-compatibility.json');
    if (fs.existsSync(compatibilityPath)) {
        return fs.readFileSync(compatibilityPath, 'utf-8');
    }
    return null;
}
exports.getCompatibilityForLibraryName = getCompatibilityForLibraryName;

"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getVersionsIncludedFor = exports.getVersionsForLibrary = void 0;
const child_process_1 = require("child_process");
const fs = __importStar(require("fs"));
const os = __importStar(require("os"));
const path = __importStar(require("path"));
const compatibility_utils_1 = require("./compatibility-utils");
const file_utils_1 = require("./file-utils");
function getVersionsForLibrary(libraryName, npmManager = 'npm') {
    try {
        const npmLsOutput = (0, child_process_1.execSync)(`${npmManager} view ${libraryName} versions --json`, { encoding: 'utf8' });
        const versions = JSON.parse(npmLsOutput);
        return versions;
    }
    catch (error) {
        throw new Error(`Failed to retrieve versions for ${libraryName}: ${error}`);
    }
}
exports.getVersionsForLibrary = getVersionsForLibrary;
/**
 * Processes an NPM library to either list available versions or get module mappings
 * @param libraryName - The name of the NPM library (e.g., "@amazon-devices/react-native-gesture-handler")
 * @param version - Optional version string. If not provided, lists available versions
 * @returns
 */
function getVersionsIncludedFor(libraryName, version, npmManager = 'npm') {
    let tempDir;
    try {
        tempDir = fs.mkdtempSync(path.join(os.tmpdir(), 'kmmb-'));
        (0, file_utils_1.extractTarball)(npmManager, libraryName, version, tempDir);
        const packageDir = path.join(tempDir, 'package');
        const compatibilityPath = path.join(packageDir, 'kepler-compatibility.json');
        const content = fs.readFileSync(compatibilityPath, 'utf8');
        if (!content) {
            throw new Error(`No compatibility information found for ${libraryName}@${version}`);
        }
        const parsedOutput = (0, compatibility_utils_1.parseCompatibilityInfo)(content, libraryName);
        if (!parsedOutput) {
            throw new Error(`An error occurred parsing compatibility info for ${libraryName}@${version}`);
        }
        return parsedOutput;
    }
    catch (error) {
        throw new Error(`Failed to process ${libraryName}@${version}: ${error}`);
    }
    finally {
        if (tempDir) {
            fs.rmSync(tempDir, { recursive: true, force: true });
        }
    }
}
exports.getVersionsIncludedFor = getVersionsIncludedFor;

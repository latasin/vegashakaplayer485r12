import { KeplerCompatibilityMap, ModulesDeclared } from '../types';
/**
 * Checks if a version is included in a given NPM version range.
 * @param version - The version to check (e.g., "2.1.0")
 * @param range - The NPM version range (e.g., "^2.0.0", "~2.13.0")
 * @returns True if the version is included in the range, false otherwise
 */
export declare function isVersionInRange(version: string, range: string): boolean;
/**
 * Finds the intersection of two NPM version ranges.
 * @param range1 - The first NPM version range (e.g., "^2.0.0")
 * @param range2 - The second NPM version range (e.g., "~2.13.0")
 * @returns The intersection of the two ranges (e.g., "~2.13.0")
 */
export declare function getIntersectionRange(range1: string, range2: string): string;
/**
 * Checks if a version is larger than some other version.
 * @param version - The version to check (e.g., "2.1.0")
 * @param version2 - The other version to check (e.g., "2.4.0")
 * @returns True if the version is greater than version2, false otherwise
 */
export declare function isVersionLargerThan(version: string, version2: string): boolean;
/**
 * Checks if a two versions are equal.
 * @param version - The version to check (e.g., "2.1.0")
 * @param version2 - The other version to check (e.g., "2.4.0")
 * @returns True if the version is equal to version2, false otherwise
 */
export declare function isVersionEqual(version: string, version2: string): boolean;
/**
 * Check if a version is an exact version (not a range).
 * @param versionOrRange
 * @returns True if the version is an exact version, false otherwise
 */
export declare function isExactVersion(versionOrRange: string): boolean;
export declare function setNeedsForLibrary(libraryName: string, manifestMap: KeplerCompatibilityMap, appDefinedNeed: string): {
    version: string;
    source: string;
};
export declare function getHighestVersionInMap(libraryName: string, versionMap: Record<string, ModulesDeclared>): string;

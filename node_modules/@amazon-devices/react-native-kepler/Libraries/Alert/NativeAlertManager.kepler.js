/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @flow
 * @format
 */

import type {KeplerTurboModule} from '../TurboModule/KeplerTurboModule';
import * as TurboModuleRegistry from '../TurboModule/TurboModuleRegistry';

export type AlertButton = {
  text?: string,
  onPress?: ?Function,
  ...
};

export type AlertOptions = {
  cancelable?: ?boolean,
  onDismiss?: ?() => void,
  ...
};

export interface Spec extends KeplerTurboModule {
  +alert: (
    title: string,
    message: string,
    buttons: Array<AlertButton>,
    options: AlertOptions,
    onPressDelegator: (buttonIdx: number) => void,
    onDismiss: () => void,
  ) => void;
}

export default (TurboModuleRegistry.getEnforcing<Spec>('Alert'): Spec);

/**
  * Represents the different types of components of the application.
  */
export declare enum ComponentType {
    INTERACTIVE = 0,
    SERVICE = 1,
    TASK = 2
}
/**
  * Represents the component of application, which includes the name and type.
  */
export interface IComponentInstance {
    name: string;
    type: ComponentType;
    id: string;
}

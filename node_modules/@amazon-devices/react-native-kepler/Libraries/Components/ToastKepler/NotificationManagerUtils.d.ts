import type { IKindVisitor, ILaunchTargetVisitor, ToastKindDescriptor } from './NativeNotificationManager';
/**
 * Type of launch target used to describe an interactive app.
 * This target will be launched when a notification is
 * interacted with.
 */
export declare class AppLaunchTarget {
    /**
     * Returns the uri of the app to be launched.
     * Includes any additional args potentially required
     * when the instance has started.
     * Example: "orpheus://com.amazon.lcm.test.main"
     */
    appUri: string;
    /**
     * Gets the text to be used to identify the launch target
     */
    title: string;
    /**
     * Icon Uri to be displayed as part of the launch target
     */
    iconUri: string;
    constructor(appUri: string, title: string, iconUri: string);
    /**
     * @param visitor Implementation of a visitor
     * implementing functions that perform different tasks
     * depending on the kind of launch target
     */
    visit(visitor: ILaunchTargetVisitor): any;
}
export declare class ToastKind {
    kindDescriptor: ToastKindDescriptor;
    /**
     * Used to visit the 'kind' in a type safe way
     * @param visitor Implementation of a visitor
     * implementing functions that perform different tasks
     * depending on the type of 'kind'
     */
    visit(visitor: IKindVisitor): void;
}

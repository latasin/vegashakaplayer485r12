import type { TurboModule } from '../TurboModule/RCTExport';
export interface KPNativeLogger extends TurboModule {
    /**
     * @brief Log a message with the specified logging level
     *
     * @param level a KPLogLevel string.
     * @param message the message to print
     *
     * @discussion The original intent was to support an argument
     * ...args: any[] to allow formatting message on the native side.
     * This was problematic because native receives an array, while
     * the method it needs to call in C++ expects variadic arguments,
     * There's no clearly supported way to convert an array to pass
     * to a function like that.
     * So we are using a custom formatter to format on this side instead.
     */
    log(level: string, message: string): void;
}
declare const _default: KPNativeLogger;
export default _default;

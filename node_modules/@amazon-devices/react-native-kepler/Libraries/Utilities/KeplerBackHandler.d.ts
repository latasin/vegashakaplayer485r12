declare type BackPressEventName = 'backPress' | 'hardwareBackPress';
export interface KeplerBackHandler {
    /**
     * Exits the app.
     */
    exitApp: () => void;
    /**
     * Adds an event listener for the specified event.
     * @param eventName - The name of the event to listen for.
     * @param handler - The function to be called when the event is triggered.
     * @returns An object with a `remove` function to remove the event listener.
     */
    addEventListener: (eventName: BackPressEventName, handler: () => boolean | void) => {
        remove: () => void;
    };
    /**
     * Removes an event listener for the specified event.
     * @param eventName - The name of the event to remove the listener for.
     * @param handler - The function that was registered as the event handler.
     */
    removeEventListener: (eventName: BackPressEventName, handler: () => boolean | void) => void;
}
/**
 * Hook to return the KeplerBackHandler instance
 * Usage:
 * import { KeplerBackHandler, useKeplerBackHandler } from '@amazon-devices/react-native-kepler';
 * ...
 * const keplerBackHandler: KeplerBackHandler = useKeplerBackHandler();
 *  keplerBackHandler.exitApp();
 */
export declare const useKeplerBackHandler: () => KeplerBackHandler;
export {};

/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * Portions of this file are copyright (c) 2021 Amazon.com, Inc. or its affiliates.  All rights reserved.
 *
 * PORTIONS OF THIS FILE ARE AMAZON PROPRIETARY/CONFIDENTIAL.  USE IS SUBJECT TO LICENSE TERMS.
 *
 * Amazon modifications are indicated by [amznmod_* comments].
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @flow
 * @format
 */

/**
 * CHANGELOG:
 * 36d57fab - Changed TMs to use Kepler TM interface
 * a482431d - Added attributions to modified react-native files
 */

// amznmod_react - Changed to use Kepler TM interface
import type {KeplerTurboModule} from '../TurboModule/KeplerTurboModule'
import * as TurboModuleRegistry from '../TurboModule/TurboModuleRegistry';
import type {EventSubscription} from "../vendor/emitter/EventEmitter";

// amznmod_react - Changed to use Kepler TM interface
export interface Spec extends KeplerTurboModule {
  +connect: (
    url: string,
    protocols: ?Array<string>,
    options: {|headers?: Object|},
    socketID: number,
  ) => void;
  +send: (message: string, forSocketID: number) => void;
  +sendBinary: (base64String: string, forSocketID: number) => void;
  +ping: (socketID: number) => void;
  +close: (code: number, reason: string, socketID: number) => void;

  // RCTEventEmitter
  +addListener: (eventName: string) => EventSubscription;
  +removeListeners: (count: number) => void;
}

// amznmod_react - Changed according to comment below
// On kepler this TurboModule is JSI's HostObject 
// This makes it impossible to assign anything to such "Object"
// WebSocketInterceptor heavily depends on that ability
// wrapping this TurboModule in JS's objects makes it way simpler
// and safer that handling those assigns on C++ side.

const wrapper = Object.create((TurboModuleRegistry.getEnforcing<Spec>(
  'WebSocketModule',
): Spec));
export default wrapper;

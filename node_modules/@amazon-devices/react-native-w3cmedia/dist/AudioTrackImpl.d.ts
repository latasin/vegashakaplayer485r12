import { AudioTrack } from './interface/MediaTypes';
import * as NativeInterfaces from "./turbo-modules/NativeInterface";
export declare class AudioTrackImpl implements AudioTrack {
    private audioTrack;
    private mediaElement;
    constructor(audioTrack: NativeInterfaces.NativeAudioTrack, mediaElement: NativeInterfaces.NativeMediaElement);
    get id(): string;
    get kind(): string;
    get label(): string;
    get language(): string;
    get enabled(): boolean;
    set enabled(enabled: boolean);
}

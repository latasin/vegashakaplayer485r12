import { AudioTrackImpl } from './AudioTrackImpl';
import { AddEventListenerOptions, EventListener, EventListenerOptions, EventTarget } from './interface/EventTargetInterface';
import { AudioTrack, AudioTrackList } from './interface/MediaTypes';
/**
 * Implement AudioTrackList interface
 */
export declare class AudioTrackListImpl implements EventTarget, AudioTrackList {
    private eventEmitter;
    private audioTracks;
    constructor();
    get length(): number;
    set onchange(listener: EventListener);
    set onaddtrack(listener: EventListener);
    set onremovetrack(listener: EventListener);
    getTrackById(id: string): AudioTrack | undefined;
    /**
     * Emits the Track list related events
     * @param eventName name of the track
     * @param audioTrack track
     */
    emitEvent(eventName: string, audioTrack: AudioTrackImpl): void;
    /**
     * Adds a track to the tracks list
     */
    addTrack(track: AudioTrackImpl): void;
    removeTrack(id: string): void;
    addEventListener(type: string, listener: EventListener, options?: AddEventListenerOptions | boolean): void;
    removeEventListener(type: string, listener: EventListener, options?: EventListenerOptions | boolean): void;
}

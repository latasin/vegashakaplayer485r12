import { AddEventListenerOptions, EventListener, EventListenerOptions, EventTarget } from "./interface/EventTargetInterface";
import { AppendMode, SourceBuffer } from "./interface/MediaSourceInterface";
import { AudioTrackList, TextTrackList, TimeRanges, VideoTrackList } from "./interface/MediaTypes";
import * as NativeInterfaces from "./turbo-modules/NativeInterface";
import { MediaSource } from "./interface";
import { EventAttributes } from "./EventAttributes";
/**
 * This class implements SourceBuffer interface.
 * It uses EventEmitter to emit events
 */
export declare class SourceBufferImpl extends EventAttributes implements SourceBuffer, EventTarget {
    private eventEmitter;
    private MEDIA_SEGMENTS;
    private MEDIA_SEQUENCE;
    private mediaElement;
    private sourceBufferId;
    private mimeType;
    private audioTrackList;
    private videoTrackList;
    private textTrackList;
    private mediaSource;
    private _onupdatestart;
    private _onupdate;
    private _onupdateend;
    private _onerror;
    private _onabort;
    private MEDIA_EVENT_SOURCE_AUDIO_TRACKLIST;
    private MEDIA_EVENT_SOURCE_VIDEO_TRACKLIST;
    private MEDIA_EVENT_SOURCE_TEXT_TRACKLIST;
    private trackListEventHandler;
    constructor(mediaElement: NativeInterfaces.NativeMediaElement | null, mediaSource: MediaSource, sourceBufferId: string, type: string);
    /**
     * @hidden
     * Checks whether sourcebuffer Exists or not.
     */
    private sourceBufferExists;
    /**
     * @hidden
     * Throw InvlaidStateError if 'updating' is true
     */
    private isUpdating;
    addEventListener(type: string, listener: EventListener, options?: AddEventListenerOptions | boolean): void;
    removeEventListener(type: string, listener: EventListener, options?: EventListenerOptions | boolean): void;
    get mode(): AppendMode;
    set mode(mode: AppendMode);
    get updating(): boolean;
    get buffered(): TimeRanges;
    get timestampOffset(): number;
    set timestampOffset(offset: number);
    get audioTracks(): AudioTrackList;
    get videoTracks(): VideoTrackList;
    get textTracks(): TextTrackList;
    get appendWindowStart(): number;
    set appendWindowStart(wndStart: number);
    get appendWindowEnd(): number;
    set appendWindowEnd(wndEnd: number);
    /**
     * For additional information, see
     * https://www.w3.org/TR/media-source-2/#dom-sourcebuffer-onupdatestart
     */
    set onupdatestart(listener: EventListener);
    /**
     * For additional information, see
     * https://www.w3.org/TR/media-source-2/#dom-sourcebuffer-onupdate
     */
    set onupdate(listener: EventListener);
    /**
     * For additional information, see
     * https://www.w3.org/TR/media-source-2/#dom-sourcebuffer-onupdateend
     */
    set onupdateend(listener: EventListener);
    /**
     * For additional information, see
     * https://www.w3.org/TR/media-source-2/#dom-sourcebuffer-onerror
     */
    set onerror(listener: EventListener);
    /**
     * For additional information, see
     * https://www.w3.org/TR/media-source-2/#dom-sourcebuffer-onabort
     */
    set onabort(listener: EventListener);
    appendBuffer(data: ArrayBuffer | Uint8Array): void;
    abort(): void;
    changeType(type: string): void;
    checkforValidNumber(value: number): boolean;
    remove(start: number, end: number): void;
    getId(): string;
    handleEvent(event: NativeInterfaces.NativeMediaElementEvent): void;
}

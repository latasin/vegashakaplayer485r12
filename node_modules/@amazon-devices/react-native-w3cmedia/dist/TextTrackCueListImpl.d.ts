import { TextTrackCueList, TextTrackCue } from './interface/MediaTypes';
/**
 * Implement TextTrackCueList interface
 */
export declare class TextTrackCueListImpl implements TextTrackCueList {
    private eventEmitter;
    private textTrackCueList;
    private cueMap;
    constructor();
    get length(): number;
    getCueById(id: string): TextTrackCue | undefined;
    /**
     * Adds a cue to the TTCue list
     */
    addCue(cue: TextTrackCue): void;
    /**
     * removes a cue from the TTCue list
     */
    removeCue(cue: TextTrackCue): void;
    removeAllCues(): void;
    private isEmpty;
    private checkAndAppend;
    /**
     * In-order cues are not guaranteed, so find the insertion index
     * https://html.spec.whatwg.org/multipage/media.html#text-track-cue-order
     * If multiple cues have the same start and end timestamps, then
     * insert this cue after the last matching cue.
     */
    private findInsertionIdx;
    findCueIndex(cue: TextTrackCue): number;
    private checkRelativePosition;
}

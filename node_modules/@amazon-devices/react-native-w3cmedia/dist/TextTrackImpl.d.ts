import { TextTrack, TextTrackCue, TextTrackCueList, TextTrackKind, TextTrackMode } from './interface/MediaTypes';
import * as NativeInterfaces from "./turbo-modules/NativeInterface";
import { AddEventListenerOptions, EventListener, EventListenerOptions, EventHandler, Event } from './interface/EventTargetInterface';
import { MediaPlayer } from './MediaPlayer';
/**
 * @TODO We need to have separate implementations for
 * JS and native TextTracks, if we want to support both.
 * This looks messed up right now.
 */
export declare class TextTrackImpl implements TextTrack {
    /**
     * We support multiple instances of MediaElement.
     * Let JS TextTrack ids be unique across MediaElements as well.
     */
    private static orderId;
    static readonly NATIVE_TRACK_ID_PREFIX: string;
    static readonly JS_TRACK_ID_PREFIX: string;
    /**
     * @TODO : Get rid of native TextTracks.
     * Try using JS implementation itself.
     * */
    private textTrack;
    private mediaElement;
    private textTrackCueList;
    private activeCueList;
    private _id;
    /**
     * This is the creation order and
     * not the TrackList insertion order
     */
    private _orderId;
    private _kind;
    private _label;
    private _language;
    private _inBandMetadataTrackDispatchType;
    private _mode;
    private _readinessState;
    private eventEmitter;
    private _mediaPlayer;
    /**
     * extra added args
     */
    private _uri?;
    private _mimeType?;
    constructor(mediaPlayer: MediaPlayer, mediaElement: NativeInterfaces.NativeMediaElement | null, textTrack?: NativeInterfaces.NativeTextTrack, kind?: TextTrackKind, label?: string, language?: string, uri?: string, mimeType?: string);
    get cues(): TextTrackCueList;
    get activeCues(): TextTrackCueList;
    /**
     * @param {TextTrackCue} cue
     */
    addCue(cue: TextTrackCue): void;
    removeCue(cue: TextTrackCue): void;
    get id(): string;
    get orderId(): number;
    get kind(): TextTrackKind;
    get label(): string;
    get language(): string;
    /**
     * @TODO implement
     */
    get inBandMetadataTrackDispatchType(): string;
    get mode(): TextTrackMode;
    set mode(aMode: TextTrackMode);
    oncuechange: EventHandler;
    addEventListener(type: string, listener: EventListener, options?: AddEventListenerOptions | boolean): void;
    removeEventListener(type: string, listener: EventListener, options?: EventListenerOptions | boolean): void;
    /**
     * @TODO Handle cuechange event
     * @param event
     * @returns
     */
    dispatchEvent(event: Event): boolean;
    refresh(): void;
}

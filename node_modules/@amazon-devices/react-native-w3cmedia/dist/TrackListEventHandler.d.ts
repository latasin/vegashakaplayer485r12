import { AudioTrackListImpl } from "./AudioTrackListImpl";
import { TextTrackListImpl } from "./TextTrackListImpl";
import * as NativeInterfaces from "./turbo-modules/NativeInterface";
import { VideoTrackListImpl } from "./VideoTrackListImpl";
export declare class TrackListEventHandler {
    /**
     * Handles audio track list event. It adds, removes or changes the video tracks in the
     * audio track list, and pases the event to audio track list so that it can emit it
     * to its listeners
     */
    handleAudioTrackListEvent(nativeEvent: NativeInterfaces.NativeMediaElementEvent, audioTrackList: AudioTrackListImpl, mediaElement: NativeInterfaces.NativeMediaElement | null): void;
    /**
     * Handles video track list event. It adds, removes or changes the video tracks in the
     * video track list, and pases the event to audio track list so that it can emit it
     * to its listeners
     */
    handleVideoTrackListEvent(nativeEvent: NativeInterfaces.NativeMediaElementEvent, videoTrackList: VideoTrackListImpl, mediaElement: NativeInterfaces.NativeMediaElement | null): void;
    /**
     * Handles video track list event. It adds, removes or changes the video tracks in the
     * video track list, and pases the event to audio track list so that it can emit it
     * to its listeners
     */
    handleTextTrackListEvent(nativeEvent: NativeInterfaces.NativeMediaElementEvent, textTrackList: TextTrackListImpl, mediaElement: NativeInterfaces.NativeMediaElement | null): void;
}

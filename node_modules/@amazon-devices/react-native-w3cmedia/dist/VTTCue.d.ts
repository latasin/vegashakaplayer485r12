import { TextTrack, TextTrackCue } from './interface/MediaTypes';
import { AddEventListenerOptions, EventListener, EventListenerOptions, EventHandler, Event } from './interface/EventTargetInterface';
import { VTTRegion } from './VTTRegion';
export declare type AutoKeyword = 'auto';
export declare type LineAndPositionSetting = number | AutoKeyword;
export declare type DirectionSetting = '' | 'rl' | 'lr';
export declare type LineAlignSetting = 'start' | 'center' | 'end';
export declare type PositionAlignSetting = 'line-left' | 'center' | 'line-right' | 'auto';
export declare type AlignSetting = 'start' | 'center' | 'end' | 'left' | 'right';
/**
 * @hidden
 * https://www.w3.org/TR/webvtt1/#the-vttcue-interface
 */
export declare class VTTCue implements TextTrackCue {
    private _track?;
    private _id;
    private _startTime;
    private _endTime;
    private _pauseOnExit;
    private _region?;
    private _vertical;
    private _snapToLines;
    private _line;
    private _lineAlign;
    private _position;
    private _positionAlign;
    private _size;
    private _align;
    private _text;
    private _isActive;
    private eventEmitter;
    private static cueId;
    /**
     * @track is a readonly property, can not define a setter.
     * Users do not set this while constructing the VTTCue.
     */
    constructor(startTime: number, endTime: number, text: string, track?: TextTrack);
    get track(): TextTrack;
    /**
     * Not a usual JS setter
     * @param track
     */
    setTrack(track: TextTrack): void;
    get id(): string;
    set id(cId: string);
    get startTime(): number;
    set startTime(cStartTime: number);
    get endTime(): number;
    set endTime(cEndTime: number);
    get pauseOnExit(): boolean;
    set pauseOnExit(aPauseOnExit: boolean);
    onenter: EventHandler;
    onexit: EventHandler;
    get vertical(): DirectionSetting;
    set vertical(aVerticalDirection: DirectionSetting);
    get snapToLines(): boolean;
    set snapToLines(aSnapToLines: boolean);
    get line(): LineAndPositionSetting;
    set line(aLineAndPositionSetting: LineAndPositionSetting);
    get lineAlign(): LineAlignSetting;
    set lineAlign(aLineAlign: LineAlignSetting);
    get position(): LineAndPositionSetting;
    set position(aPosition: LineAndPositionSetting);
    get positionAlign(): PositionAlignSetting;
    set positionAlign(aPositionAlign: PositionAlignSetting);
    get size(): number;
    set size(aSize: number);
    get align(): AlignSetting;
    set align(aAlign: AlignSetting);
    get text(): string;
    set text(aText: string);
    get isActive(): boolean;
    set isActive(aActive: boolean);
    get region(): VTTRegion;
    set region(aRegion: VTTRegion);
    addEventListener(type: string, listener: EventListener, options?: AddEventListenerOptions | boolean): void;
    removeEventListener(type: string, listener: EventListener, options?: EventListenerOptions | boolean): void;
    dispatchEvent(event: Event): boolean;
}

"use strict";
/*
 * Copyright (c) 2024 Amazon.com, Inc. or its affiliates.  All rights reserved.
 *
 * PROPRIETARY/CONFIDENTIAL.  USE IS SUBJECT TO LICENSE TERMS.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveIndicatorAnimationDuration = exports.resolveIndicatorTokens = exports.resolveTrackTokens = void 0;
const resolveTrackTokens = ({ tokens, size, trackColor }) => {
    if (!tokens) {
        return undefined;
    }
    const borderRadius = tokens.track?.size?.borderRadius?.[size];
    const height = tokens.track?.size?.height?.[size];
    const width = tokens.track?.size?.width?.[size];
    const backgroundColor = trackColor || tokens.track?.color?.backgroundColor;
    return {
        borderRadius,
        height,
        width,
        backgroundColor,
        flexDirection: 'row',
        overflow: 'hidden'
    };
};
exports.resolveTrackTokens = resolveTrackTokens;
const resolveIndicatorTokens = ({ tokens, size, indicatorColor }) => {
    if (!tokens) {
        return undefined;
    }
    const borderRadius = tokens.indicator?.size?.borderRadius?.[size];
    const backgroundColor = indicatorColor || tokens.indicator?.color?.backgroundColor;
    return { borderRadius, backgroundColor };
};
exports.resolveIndicatorTokens = resolveIndicatorTokens;
const resolveIndicatorAnimationDuration = ({ isIndeterminate, tokens, animationDuration }) => {
    const determinateDurationToken = tokens?.indicator?.motion?.duration?.determinate;
    const indeterminateDurationToken = tokens?.indicator?.motion?.duration?.indeterminate;
    if (animationDuration === 0) {
        return 0;
    }
    else if (!isIndeterminate) {
        return animationDuration || determinateDurationToken || 250;
    }
    else if (isIndeterminate) {
        return animationDuration || indeterminateDurationToken || 1000;
    }
    return false;
};
exports.resolveIndicatorAnimationDuration = resolveIndicatorAnimationDuration;

/// <reference types="react" />
import { ColorValue, StyleProp, ViewStyle } from 'react-native';
import { CommonProps } from '../CommonUtilities';
declare type Marker = {
    /**
     * The position is a value inclusive between 0 and the totalValue (generally
     * in milliseconds) that determines the horizontal positioning of the marker
     * on the SeekBar. The horizontal positioning is based on the ratio of the
     * provided position and totalValue of the seekBar, proportional to the width
     * of the SeekBar.
     */
    position: number;
    /**
     * Custom React Node to display for the marker
     */
    node: React.ReactNode;
};
export interface SliderProps extends CommonProps {
    /**
     * When true, disables user interaction with the component
     * @default false
     * */
    disabled?: boolean;
    /**
     * A number that represents the current value, generally in milliseconds. This
     * value range should be between 0 and the provided totalValue, both inclusive.
     * */
    currentValue: number;
    /**
     * A number that represents the total value, generally in milliseconds
     * */
    totalValue: number;
    /**
     * Function to call when the slider value changes while sliding
     * */
    onValueChange?: (value: number) => void;
    /**
     * Function to call when sliding is initiated. For D-Pad inputs, sliding is
     * initiated when the pressIn event is received for left or right D-Pad
     * events.
     * */
    onSlidingStart?: () => void;
    /**
     * Function to call when sliding is complete. For D-Pad inputs, sliding is
     * completed when the pressOut event is received for left or right D-Pad
     * events.
     * */
    onSlidingEnd?: () => void;
    /**
     * Function to call when the component is pressed. Contains a value parameter
     * that corresponds with the thumb value.
     * */
    onPress?: (value: number) => void;
    /**
     * Function to call when the rewind button is pressed using the D-Pad
     * */
    onRewindPress?: () => void;
    /**
     * Function to call when the fast forward button is pressed using the D-Pad
     * */
    onFastForwardPress?: () => void;
    /**
     * Value to move the slider thumb when using the D-Pad. This value can also be
     * negative.
     * */
    step?: number;
    /**
     * List of markers to display on the SeekBar. One marker object consists of a
     * position and a React Node for the marker.
     *
     * The position is a value inclusive between 0 and the totalValue (generally
     * in milliseconds) that determines the horizontal positioning of the marker
     * on the SeekBar. The horizontal positioning is based on the ratio of the
     * provided position and totalValue of the seekBar, proportional to the width
     * of the SeekBar.
     * */
    markers?: Marker[];
    /**
     * Custom React Node to display above the slider thumb
     * */
    displayAboveThumb?: React.ReactNode;
    /**
     * Custom React Node to display below the slider thumb
     * */
    displayBelowThumb?: React.ReactNode;
    /**
     * Function to call when the component gains focus
     * */
    onFocus?: () => void;
    /**
     * Function to call when the component loses focus
     * */
    onBlur?: () => void;
    /**
     * Custom icon for the slider thumb
     * */
    thumbIcon?: React.ReactNode;
    /**
     * Color of the current value indicator
     * */
    currentValueIndicatorColor?: ColorValue;
    /**
     * Color of the bar container
     * */
    barTrackColor?: ColorValue;
    /**
     * Style override for bar
     * */
    barStyle?: StyleProp<ViewStyle>;
    /**
     * Style override for current value indicator
     * */
    currentValueIndicatorStyle?: StyleProp<ViewStyle>;
    /**
     * Style override for timeshift indicator
     * */
    timeShiftIndicatorStyle?: StyleProp<ViewStyle>;
    /**
     * Interval duration in milliseconds when performing a D-Pad long press.
     *
     * For D-Pad inputs, a long press is triggered by holding the left or right
     * key, which intiates a rewind or fast forward action.
     * */
    longPressIntervalDuration?: number;
}
export {};

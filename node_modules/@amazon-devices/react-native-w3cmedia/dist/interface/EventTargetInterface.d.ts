import { AudioTrack, TextTrack, VideoTrack } from "./MediaTypes";
export interface EventListenerOptions {
    capture?: boolean;
}
export declare class Event {
    readonly type: string;
    readonly target: EventTarget;
    constructor();
    constructor(type: string, target: EventTarget);
}
/**
 * https://html.spec.whatwg.org/multipage/media.html#trackevent
 */
export declare class TrackEvent extends Event {
    /**
     * https://html.spec.whatwg.org/multipage/media.html#dom-trackevent-track
     */
    track: AudioTrack | VideoTrack | TextTrack;
    constructor(type: string, target: EventTarget, track: AudioTrack | VideoTrack | TextTrack);
}
export declare class MediaEncryptedEvent extends Event {
    readonly initDataType: string;
    readonly initData: ArrayBuffer;
    constructor(type: string, target: EventTarget, initDataType: string, initData: ArrayBuffer);
}
export interface AddEventListenerOptions extends EventListenerOptions {
    passive?: boolean;
    once?: boolean;
    signal?: AbortSignal;
}
export interface EventListener {
    (event?: Event | TrackEvent | MediaEncryptedEvent): void;
}
/**
 * Should it return unknown?
 * https://html.spec.whatwg.org/multipage/webappapis.html#eventhandler
 */
export declare type EventHandler = (event?: Event) => any;
/**
 * https://dom.spec.whatwg.org/#interface-eventtarget
 */
export interface EventTarget {
    /**
     * Adds an event listener.
     * @param type The event to listen to.
     * @param listener  The Event Handler function.
     * @param options Ignore.
     */
    addEventListener(type: string, listener: EventListener, options?: AddEventListenerOptions | boolean): void;
    /**
     * Removes an event listener.
     * @param type The event to listen to.
     * @param listener The Event Handler function to be removed.
     * @param options  Ignore.
     */
    removeEventListener(type: string, listener: EventListener, options?: EventListenerOptions | boolean): void;
    /**
     * Dispatches an event to the target
     * @param event The event which signals an occurrenceo.
     * @TODO Add and define in the implementations, keep it optional for now.
     */
    dispatchEvent?(event: Event): boolean;
}

/** This file is copied from ATVKeplerVideoSurface vmpf-dev branch */
import * as React from "react";
import { ViewProps } from "react-native";
interface SurfaceViewProps extends ViewProps {
    /**
     * @brief An event callback that is called when
     * `SurfaceView` is mounted to render tree.
     */
    onComponentDidMount?: Function;
    /**
     * @brief An event callback that is called when
     * `SurfaceView` is unmounted from render tree.
     */
    onComponentWillUnmount?: Function;
    /**
    * @brief An event callback that is called when the underlying native surface
     * view is created.
     * @param[in] `surfaceViewHandle`
     */
    onSurfaceViewCreated?: (surfaceViewHandle: string) => void;
    /**
     * @brief And event callback that is called when the underlying native surface
     * view is destroyed.
     * @param[in] `surfaceViewHandle` The surface view handle
     * to provide to the player to render video.
     */
    onSurfaceViewDestroyed?: (surfaceViewHandle: string) => void;
    /**
     * @brief configure the scaling mode for rendered video.
     * scalingmode suppprts following.
     * 'none' : No scaling. It preserves the input size.
     * 'fit' : Scales buffer to match window size while preserving the aspect ratio &
     *         center position.
     * 'strech': Scales buffer to fit in window, initially without cropping.
     *           Scaling up beyond the bounds of the window will stretch the dst rect
     *           and excess is cropped down to window bounds.
     *           Aspect ratio and center pos are maintained
     * 'fill':  Scales buffer to fill the entire window, preserving its aspect
     *          ratio and hiding any overflow on the top/bottom/left/right.
     * default:  'strech'
     */
    scalingmode?: 'none' | 'fit' | 'strech' | 'fill' | undefined;
}
/**
 * @brief This is a react native component that Renders video frames on the screen.
 * Use this component only when operating in pre-buffering mode using the `VideoPlayer`
 * component, where more than one player can be pre-buffered simultaneously with
 * different contents. The app then attaches the surface handle passed by the
 * `onSurfaceViewCreated` event callback to the video player instance that needs
 * to be rendered.
 */
declare class KeplerVideoSurfaceView extends React.Component<SurfaceViewProps> {
    constructor(props: SurfaceViewProps);
    /**
     * @hidden
     */
    componentDidMount(): void;
    /**
     * @hidden
     */
    componentWillUnmount(): void;
    /**
     * @hidden
     */
    render(): React.ReactNode;
}
export { SurfaceViewProps, KeplerVideoSurfaceView };

import * as React from 'react';
import { ViewProps } from 'react-native';
import { VideoPlayer } from './VideoPlayer';
/**
 * Props of KeplerVideoView component
 */
interface KeplerVideoViewProps extends ViewProps {
    /**
     * Set to true to render media controls UI
     */
    showControls?: boolean;
    /**
     * Set to `true` to render media controls UI.
     */
    showCaptions?: boolean;
    /**
     * Set to `true` to render captions.
     */
    videoPlayer: VideoPlayer;
    /**
     * @brief configure the scaling mode for rendered video.
     * scalingmode suppprts following.
     * 'none' : No scaling. It preserves the input size.
     * 'fit' : Scales buffer to match window size while preserving the aspect ratio &
     *         center position.
     * 'strech': Scales buffer to fit in window, initially without cropping.
     *           Scaling up beyond the bounds of the window will stretch the dst rect
     *           and excess is cropped down to window bounds.
     *           Aspect ratio and center pos are maintained
     * 'fill':  Scales buffer to fill the entire window, preserving its aspect
     *          ratio and hiding any overflow on the top/bottom/left/right.
     * default:  'strech'
     */
    scalingmode?: 'none' | 'fit' | 'strech' | 'fill' | undefined;
}
/**
 * @brief This is a react native component that renders the following as specified
 * by optional props set by the app developer:
 *
 * * Video frames on the screen.
 * * Closed captions and subtitles on the screen (optional).
 * * Media controls (optional).
 *
 */
declare const KeplerVideoView: {
    (props: KeplerVideoViewProps): React.JSX.Element;
    defaultProps: {
        showControls: boolean;
        showCaptions: boolean;
        scalingmode: string;
    };
};
export { KeplerVideoViewProps, KeplerVideoView };

import { VideoDecoderCapabilities, AudioDecoderCapabilities, VideoDecoderConfig, AudioDecoderConfig } from './turbo-modules/NativeInterface';
export declare type MediaCapabilities = VideoDecoderCapabilities | AudioDecoderCapabilities;
export declare type MediaConfig = AudioDecoderConfig | VideoDecoderConfig;
/**
 * The entry point for KMD apis. This class provides apis to query the media capabilities of
 * the device.
 */
export declare class KeplerMediaDescriptor {
    constructor();
    /**
     * Query the Media capabilities asynchronously.
     *
     * @param config @type {MediaConfig} which encapsulates all the attributes to be queried
     * querying capabilities.
     *
     * @returns @type {Promise} of @type {MediaCapabilities} containing all the capabilities returned
     * according to the @param config. Empty array if there is an error (input or internal) while querying the device
     * or the device doesn't support the requested  attributes.
     */
    static queryMediaCapabilities(config: MediaConfig): Promise<Array<MediaCapabilities>>;
    static queryMediaCapabilitiesSync(config: MediaConfig): Array<MediaCapabilities>;
}

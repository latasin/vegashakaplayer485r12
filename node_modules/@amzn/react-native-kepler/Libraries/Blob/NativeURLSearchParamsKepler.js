/**
 * Copyright (c) 2025 Amazon.com, Inc. or its affiliates. All Rights Reserved.
 *
 * PROPRIETARY/CONFIDENTIAL
 *
 * Use is subject to license terms.
 *
 * @flow
 * @format
 */

import type {KeplerTurboModule} from '../TurboModule/KeplerTurboModule';
import * as TurboModuleRegistry from '../TurboModule/TurboModuleRegistry';

export interface Spec extends KeplerTurboModule {
  /**
   * Parse a URL query string into an array of key-value pairs
   * @param queryString The URL query string to parse
   * @returns An array of key-value pairs
   */
  +parseQuery: (queryString: string) => Array<Array<string>>;

  /**
   * Stringify an array of key-value pairs into a URL query string
   * @param pairs An array of key-value pairs
   * @returns A URL query string
   */
  +stringifyQuery: (pairs: Array<Array<string>>) => string;
}

export default (TurboModuleRegistry.getEnforcing<Spec>('URLSearchParams'): Spec);

/**
 * Enum for system permissions.
 * Each entry represents a permission defined within the system.
 */
export declare enum PERMISSIONS {
    NET_INFO = "com.amazon.network.privilege.net-info",
    ACCESS_ACCESSORY_CONTROL_ACCOUNT = "com.amazon.acc.privilege.access",
    CONNECT_ACCESSORY_CONTROL_ACCOUNT = "com.amazon.acc.privilege.connect",
    DISCOVER_ACCESSORY_CONTROL_ACCOUNT = "com.amazon.acc.privilege.discover",
    ACCESS_COARSE_LOCATION = "com.amazon.location.privilege.access.coarse",
    ACCESS_FINE_LOCATION = "com.amazon.location.privilege.access.fine",
    RECORD_AUDIO = "com.amazon.audio.privilege.microphone.access",
    DEVICE_FRIENDLY_NAME = "com.amazon.devconf.privilege.identifiers.device-friendly-name",
    BLUETOOTH_ADVERTISE = "com.amazon.bluetooth.privilege.advertise",
    BLUETOOTH_DISCOVERABLE = "com.amazon.bluetooth.privilege.discoverable",
    READ_EXTERNAL_STORAGE = "com.amazon.external-disk.privilege.readonly",
    DISCOVERABLE_ACCESSORY_CONTROL_ACCOUNT = "com.amazon.acc.privilege.discoverable",
    CAMERA = "com.amazon.camera.privilege.access",
    READ_CALENDAR = "com.amazon.calendar.privilege.read",
    WRITE_CALENDAR = "com.amazon.calendar.privilege.write"
}
/**
 * Type for system permissions.
 * Represents a union of all permission strings from the PERMISSIONS enum.
 */
export declare type Permission = `${PERMISSIONS}`;
/**
 * Return type for system permissions.
 * Represents all possible states returned from status manager
 */
export declare type PermissionStatus = "granted" | "denied";
export declare const usePermissionsKepler: () => [(permission: Permission) => Promise<boolean>, (permission: Permission) => Promise<PermissionStatus>];

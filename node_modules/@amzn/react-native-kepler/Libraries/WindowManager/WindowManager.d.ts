import type { WindowPosition } from "./WindowManagerSpec";
/**
 * Interface for managing window resizing functionality.
 */
interface WindowManager {
    /**
     * Resizes the window to the specified absolute dimensions.
     *
     * @param width - The target width of the window in pixels. Must be within the screen bounds.
     * @param height - The target height of the window in pixels. Must be within the screen bounds.
     *
     * @remarks
     * If the provided dimensions are out of the valid screen bounds, the window size remains unchanged,
     * and warnings will be logged.
     */
    resizeWindowTo: (width: number, height: number) => void;
    /**
     * Resizes the window by adding or subtracting the specified delta values from the current dimensions.
     *
     * @param deltaWidth - The change in width of the window in pixels. Positive values increase the width,
     *                     while negative values decrease it.
     * @param deltaHeight - The change in height of the window in pixels. Positive values increase the height,
     *                      while negative values decrease it.
     *
     * @remarks
     * If the resulting dimensions are out of the valid screen bounds, the window size remains unchanged,
     * and warnings will be logged.
     */
    resizeWindowBy: (deltaWidth: number, deltaHeight: number) => void;
    /**
     * Moves the window to the specified absolute dimensions.
     *
     * @param x - The X-coordinate (horizontal) of the new top-left position.
     * @param y - The Y-coordinate (vertical) of the new top-left position.
     *
     * @remarks
     * If the resulting top-left position is out of the valid screen bounds, the window position remains unchanged,
     * and warnings will be logged.
     */
    moveTo: (x: number, y: number) => void;
    /**
     * Moves the window by the specified relative offset.
     *
     * @param deltaX - The horizontal offset (in pixels) to move the window by. Use -ve value to move left.
     * @param deltaY - The vertical offset (in pixels) to move the window by. Use -ve value to move up.
     *
     * @remarks
     * If the resulting top-left position is out of the valid screen bounds, the window position remains unchanged,
     * and warnings will be logged.
     */
    moveBy: (deltaX: number, deltaY: number) => void;
    /**
     * Retrives the current position of the window.
     * This function returns the x and y coordinates of the top-left corner
     * of the window relative to the screen's origin.
     *
     * @return An object containing:
     *         - `x`: The horizontal coordinate in pixels.
     *         - `y`: The vertical coordinate in pixels.
     */
    getPosition: () => WindowPosition;
}
export declare function useWindowManager(): WindowManager;
export {};

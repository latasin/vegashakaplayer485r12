import { SourceBufferList } from "./interface/MediaSourceInterface";
import { SourceBufferImpl } from "./SourceBufferImpl";
import { AddEventListenerOptions, EventListener, EventListenerOptions, EventTarget } from "./interface/EventTargetInterface";
import { EventAttributes } from "./EventAttributes";
/**
 * This class implements SourceBufferList and uses Proxy
 * to implement [ ] operator to access underlying soucebuffes
 * at the specified index.
 */
export declare class SourceBufferListImpl extends EventAttributes implements EventTarget, SourceBufferList {
    private sourceBuffers;
    private eventEmitter;
    private _onaddsourcebuffer;
    private _onremovesourcebuffer;
    constructor();
    addEventListener(type: string, listener: EventListener, options?: boolean | AddEventListenerOptions): void;
    removeEventListener(type: string, listener: EventListener, options?: boolean | EventListenerOptions): void;
    /** SourceBufferList implemetation */
    get length(): number;
    /**
     * @hidden
     * Handle events from native turbo module
     */
    handleEvent(eventName: string): void;
    /**
     * For additional information, see
     * https://www.w3.org/TR/media-source-2/#dom-sourcebufferlist-onaddsourcebuffer
     */
    set onaddsourcebuffer(listener: EventListener);
    /**
     * For additional information, see
     * https://www.w3.org/TR/media-source-2/#dom-sourcebufferlist-onremovesourcebuffer
     */
    set onremovesourcebuffer(listener: EventListener);
    /** public apis */
    /**
     * Gets Source buffer at index. THis needs to be called after checking
     * for array lenght by the callee
     */
    getSourceBuffer(index: number): SourceBufferImpl;
    /**
     * Adds a source buffer to the list
     * @param sourceBuffer: SourceBufferImpl object that will be added to the list
     */
    addSourceBuffer(sourceBuffer: SourceBufferImpl): void;
    /**
     * Removes a source buffer from the list
     * @param id Unique id of the source buffer that needs to be removed
     */
    removeSourceBuffer(id: string): void;
    /**
     * Removes all source buffers from the list
     */
    clear(): void;
}

import { AddEventListenerOptions, EventListener, EventListenerOptions } from './interface/EventTargetInterface';
import { TextTrack, TextTrackList } from "./interface/MediaTypes";
import { TextTrackImpl } from "./TextTrackImpl";
export declare class TextTrackListImpl implements TextTrackList {
    private eventEmitter;
    textTracks: Array<TextTrackImpl>;
    constructor();
    addEventListener(type: string, listener: EventListener, options?: AddEventListenerOptions | boolean): void;
    removeEventListener(type: string, listener: EventListener, options?: EventListenerOptions | boolean): void;
    get length(): number;
    /**
     * https://html.spec.whatwg.org/multipage/media.html#text-track-api
     */
    set onchange(listener: EventListener);
    set onaddtrack(listener: EventListener);
    set onremovetrack(listener: EventListener);
    getTrackById(id: string): TextTrack | undefined;
    /**
     * Emits the Track list related events
     * @param eventName name of the track
     * @param textTrack track
     */
    emitEvent(eventName: string, textTrack: TextTrack): void;
    /**
     * Adds a track to the tracks list
     */
    addTrack(track: TextTrackImpl): void;
    removeTrack(id: string): void;
    private findTrackIndex;
}

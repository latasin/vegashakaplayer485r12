import { TextTrackCue } from './interface/MediaTypes';
import { VTTCue } from './VTTCue';
export declare class TimeMarchesOnAlgo {
    /**
     * New ques
     * current position
     * earliest position
     **/
    private _lastCurrentPosition;
    private _allCues;
    constructor();
    getLastRunPosition(): number;
    /**
     * @CHECK Player will explicitly trim the TextTracks or
     * it needs to be done implicitly??
     * @param cue
     */
    removeCue(cue: TextTrackCue): void;
    /**
     * @TODO Rework on this using TextTrackCueManager
     * https://html.spec.whatwg.org/multipage/media.html#time-marches-on
     * @param currentTime : Current playback Position
     */
    run(captionViewHandle: string, currentTime: number, newlyIntroducedCues?: Set<VTTCue>): void;
    private updateCuesLists;
    private prepareTaskList;
    private sortTaskList;
    private processTaskList;
    private informAffectedTracks;
    private updateActiveCues;
    private renderActiveCues;
    /**
     * In-order cues are not guaranteed, so sort the cues
     * https://html.spec.whatwg.org/multipage/media.html#text-track-cue-order
     * @TODO fix this is not in the required order.
     */
    private sortList;
}

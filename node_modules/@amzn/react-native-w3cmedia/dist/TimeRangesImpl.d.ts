import { TimeRanges } from "./interface/MediaTypes";
import * as NativeInterfaces from "./turbo-modules/NativeInterface";
export declare class TimeRangesImpl implements TimeRanges {
    private nativeTimeRanges;
    constructor(nativeTimeRanges: NativeInterfaces.NativeTimeRange[] | null);
    get length(): number;
    start(index: number): number;
    end(index: number): number;
}

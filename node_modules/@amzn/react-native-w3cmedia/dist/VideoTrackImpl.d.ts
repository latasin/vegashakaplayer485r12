import { VideoTrack } from "./interface/MediaTypes";
import * as NativeInterfaces from "./turbo-modules/NativeInterface";
export declare class VideoTrackImpl implements VideoTrack {
    private videoTrack;
    private mediaElement;
    constructor(videoTrack: NativeInterfaces.NativeVideoTrack, mediaElement: NativeInterfaces.NativeMediaElement);
    get id(): string;
    get kind(): string;
    get label(): string;
    get language(): string;
    get selected(): boolean;
    set selected(selected: boolean);
}

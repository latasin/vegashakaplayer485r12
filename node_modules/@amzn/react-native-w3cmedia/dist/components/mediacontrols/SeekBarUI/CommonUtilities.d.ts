import { AccessibilityProps, ViewStyle } from 'react-native';
import { SizeVariantAll } from './SizeVariantTypes';
export interface CommonProps extends AccessibilityProps {
    testID?: string;
}
export interface BaseComponentProps {
    /**
     * A string describing the component.
     * Voice over reads this string when the user selects the item.
     */
    accessibilityLabel?: string;
    /**
     * Used to provide additional style properties to the component.
     */
    style?: ViewStyle;
    /**
     * Used to locate this view in end-to-end tests.
     */
    testID?: string;
    /**
     * Used to locate this view from native classes.
     */
    nativeID?: string;
}
export declare type SizeMap<Type = number> = {
    [size in SizeVariantAll]?: Type;
};
export declare type SizeMapStringNumber = SizeMap<number | string>;

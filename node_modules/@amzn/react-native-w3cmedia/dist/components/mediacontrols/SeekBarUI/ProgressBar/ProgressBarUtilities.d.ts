import { ThemeType } from '../Theme/ThemeUtilities';
import { ColorValue, StyleProp, ViewStyle } from 'react-native';
import { SizeVariantAll } from '../SizeVariantTypes';
import { CommonProps, SizeMap, SizeMapStringNumber } from '../CommonUtilities';
export declare const progressBarTokens: (type: ThemeType) => ProgressBarTokens;
export interface ProgressBarProps extends CommonProps {
    /**
     * Number between 0 and 100 that represents % progressed.
     * When undefined, an indeterminate indicator is shown.
     * */
    progress?: number;
    /**
     * Duration of the progress animation, in milliseconds
     * @default 250ms if determinate, 1000ms if indeterminate
     * */
    animationDuration?: number;
    /**
     * Color of the progress indicator
     * */
    indicatorColor?: ColorValue;
    /**
     * Color of the progress container
     * */
    trackColor?: ColorValue;
    /**
     * Size of progressbar. Supports 'xs', 'sm', 'md', 'lg', 'xl', 'xxl'
     * @default 'md'
     * */
    size?: SizeVariantAll;
    /**
     * Style override for bar
     * */
    barStyle?: StyleProp<ViewStyle>;
    /**
     * Style override for indicator
     * */
    indicatorStyle?: StyleProp<ViewStyle>;
}
interface BarTokens {
    color?: {
        backgroundColor?: ColorValue;
    };
    size?: {
        borderRadius?: SizeMap;
    };
}
export interface ProgressBarTokens extends BarTokens {
    track?: BarTokens & {
        size?: {
            height?: SizeMapStringNumber;
            width?: SizeMapStringNumber;
        };
    };
    indicator?: BarTokens & {
        motion?: {
            duration?: {
                determinate: number;
                indeterminate: number;
            };
        };
        size?: {
            width?: {
                indeterminate?: number;
            };
        };
    };
    buffer?: BarTokens;
    thumb?: {
        color?: {
            backgroundColor?: {
                enabled?: ColorValue;
                disabled?: ColorValue;
            };
        };
        size?: SizeMap & {
            opacity?: {
                disabled?: number;
            };
        };
    };
}
export {};

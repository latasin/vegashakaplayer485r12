import { ColorValue, ViewStyle } from 'react-native';
import { SizeVariantAll } from '../SizeVariantTypes';
import { ProgressBarTokens } from './ProgressBarUtilities';
interface TrackResolverParams {
    tokens?: ProgressBarTokens;
    trackColor?: ColorValue;
    size: SizeVariantAll;
}
interface IndicatorResolverParams {
    tokens?: ProgressBarTokens;
    indicatorColor?: ColorValue;
    size: SizeVariantAll;
}
interface IndicatorAnimationDurationResolverParams {
    isIndeterminate: boolean;
    tokens?: ProgressBarTokens;
    animationDuration?: number;
}
export declare const resolveTrackTokens: ({ tokens, size, trackColor }: TrackResolverParams) => ViewStyle | undefined;
export declare const resolveIndicatorTokens: ({ tokens, size, indicatorColor }: IndicatorResolverParams) => ViewStyle | undefined;
export declare const resolveIndicatorAnimationDuration: ({ isIndeterminate, tokens, animationDuration }: IndicatorAnimationDurationResolverParams) => number | false;
export {};

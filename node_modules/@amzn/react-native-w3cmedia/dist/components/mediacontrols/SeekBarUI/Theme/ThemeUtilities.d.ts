import React from 'react';
import { ProgressBarTokens } from '../ProgressBar/ProgressBarUtilities';
export interface Theme {
    progressBar?: ProgressBarTokens;
}
export declare const defaultThemes: {
    light: Theme;
    dark: Theme;
};
export declare type ThemeType = 'light' | 'dark';
export declare const getDefaultTheme: (type?: ThemeType) => Theme;
export interface ThemeContextType<T extends Theme = Theme> {
    theme: T;
    setTheme: (theme: T) => void;
}
export declare const defaultContext: {
    theme: Theme;
    setTheme: () => void;
};
export declare const ThemeContext: React.Context<ThemeContextType<Theme>>;
/**
 *
 * The `useTheme` hook returns the current theme. If the theme is not set, or
 * useTheme is called outside the context of the ThemeProvider the default theme
 * will be returned. The theme can also be set using the `setTheme` function
 * provided by the {@link useSetTheme `useSetTheme`} hook.
 * @returns theme
 */
export declare const useTheme: <T extends Theme = Theme>() => T;

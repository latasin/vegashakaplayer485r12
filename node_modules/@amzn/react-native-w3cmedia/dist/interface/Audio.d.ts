import * as React from 'react';
import { ViewProps } from 'react-native';
import { Media } from '../Media';
import { MediaProps } from './MediaProps';
import { AudioContentType, AudioUsageType } from "./MediaTypes";
import { MediaPlayerDeInitStatus } from '../turbo-modules/NativeInterface';
export interface AudioProps {
    /**
     * @brief Type of audio to playe.
     */
    audioType?: AudioContentType;
    /**
     * @param audioUsage  Reason for audio playback.
     */
    audioUsage?: AudioUsageType;
    /**
     * @brief Specifies whether to show captions.
     */
    showCaptions?: boolean;
}
/**
 * @hidden
 */
export interface AudioState {
}
/**
 * A react native component that Implements the `HTMLAudioElement` interface that extends
 * `HTMLMediaElement` interface..
 */
export declare class Audio extends Media<AudioProps, AudioState> {
    private isMediaPlayerInitialized;
    constructor(props: AudioProps & MediaProps & ViewProps);
    /**
     * @hidden
     */
    componentDidMount(): void;
    /**
     * @hidden
     */
    componentWillUnmount(): void;
    /**
     * @hidden
     */
    componentDidUpdate(prevProps: AudioProps & MediaProps): void;
    /**
     * @brief DeInitiailzes the player synchronously
     * @param timeoutInMs timeout threshold set by client
     * @returns MediaPlayerDeInitStatus
     */
    destroyMediaPlayerSync(timeoutInMs: number): MediaPlayerDeInitStatus;
    /**
     * @hidden
     */
    render(): React.JSX.Element;
}

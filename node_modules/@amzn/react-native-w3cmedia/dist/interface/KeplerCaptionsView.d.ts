import * as React from 'react';
import { ViewProps } from 'react-native';
/**
 * Input props for KeplerCaptionsView
 */
interface CaptionViewProps extends ViewProps {
    /**
     * @brief An event callback that is called when the underlying native captions
     * view is created.
     * This event callback is called when the component
     * is added to the render tree.
     * Apps are expected to pass the handle to player
     * thorugh `setCaptionViewHandle` API.
     * @param captionsViewHandle The handle to the captions view
     * that must be passed to the `AudioPlayer` or `VideoPlayer`
     * through the `setCaptionViewHandle` methods.
     */
    onCaptionViewCreated?: (captionsViewHandle: string) => void;
    /**
     * @brief An event callback that is called when the underlying native captions
     * view is destroyed.
     * This event callback is called when the component
     * is removed to the render tree.
     * Apps are expected to clear the handle to player
     * thorugh `clearCaptionViewHandle` API.
     * @param captionsViewHandle The handle to the captions view
     * that must be passed to the `AudioPlayer` or `VideoPlayer`
     * through the `setCaptionViewHandle` methods.
     */
    onCaptionViewDestroyed?: (captionsViewHandle: string) => void;
    /**
     * Set to true to show the captions, false otherwise.
     */
    show?: boolean;
}
/**
 * @brief This is a react native component that closed captions and subtitles on the
 * screen. Use the component only when operating in pre-buffering mode using `VideoPlayer`
 * and `AudioPlayer` components.

 * Apps attach the captions handle passed by `onCaptionViewCreated` event callback to
 * the `VideoPlayer` & `AudioPlayer` by calling the `setCaptionViewHandle` function so that
 * the player is able to render captions or subtitles on screen.
 */
declare class KeplerCaptionsView extends React.Component<CaptionViewProps> {
    constructor(props: CaptionViewProps);
    /**
     * @hidden
     */
    render(): React.ReactNode;
}
export { CaptionViewProps, KeplerCaptionsView };

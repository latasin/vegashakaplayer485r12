export declare class MimeType {
    mType: string;
    mSubtype: string;
    mEssence: string;
    mParameters: Map<string, string>;
    mMimeString: string;
    constructor(mimeStr: string);
    get type(): string;
    get subType(): string;
    get essence(): string;
    getParameter(key: string): string;
    getParameterCount(): number;
    get mimeString(): string;
    /**
     * https://mimesniff.spec.whatwg.org/#parsing-a-mime-type
     *
     * @param mimeStr @type {string} the mime string from application.
     * @returns @type {MimeType} if the provided mime is valid, null otherwise.
     */
    static parseMimeString(mimeStr: string): MimeType;
}

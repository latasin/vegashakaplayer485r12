export declare function checkSupportedVideoContainers(mimeType: string): boolean;
export declare function checkSupportedAudioContainers(mimeType: string): boolean;
export declare function removeLeadingAndTrailingHTTPWhitespace(str: string): string;
export declare function solelyContainsHTTPTokenCodePoints(str: string): boolean;
export declare function removeTrailingHTTPWhitespace(str: string): string;
export declare function isHTTPWhitespaceChar(char: string): boolean;
export declare function soleyContainsHTTPQuotedStringTokenCodePoints(str: string): boolean;
/**
 * To collect an HTTP quoted string from a string input, given a position variable
 * position and an optional boolean extract-value (default false):
 * Note: we dont use extract-value explicitly since we need it to be true always.
 *
 * https://fetch.spec.whatwg.org/#collect-an-http-quoted-string
 *
 * @param input @type {string} from which output needs to be extracted.
 * @param position @type {number} current position during parsing the mime string.
 *                 (should be at the location of " in parameter map)
 * @returns @type {string} which is within the quotes and @type {number} which is the position of the end quote
 */
export declare function collectHTTPQuotedString(input: string, position: number): [string, number];

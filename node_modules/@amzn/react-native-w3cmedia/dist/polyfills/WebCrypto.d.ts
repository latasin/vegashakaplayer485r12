import { AlgorithmIdentifier, BufferSource, CryptoKey, KeyFormat, KeyUsage } from "../turbo-modules/NativeInterface";
export declare class WebCrypto {
    static randomUUID(): string;
    static subtle: {
        new (): {};
        importKey(format: KeyFormat, data: ArrayBuffer | Uint8Array | DataView, algorithm: AlgorithmIdentifier, extractable: boolean, keyUsages: Array<KeyUsage>): Promise<CryptoKey>;
        decrypt(algorithm: AlgorithmIdentifier, key: CryptoKey, data: BufferSource): Promise<any>;
    };
}
